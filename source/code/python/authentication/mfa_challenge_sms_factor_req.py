# Setting a message is optional, set a custom message only if you want to
# overwrite the default message. Make sure you use a "${code}" placeholder
# in your custom message.
challenge = sms_factor.challenge_factor(message='For the sake of example, your code is ${code}')
